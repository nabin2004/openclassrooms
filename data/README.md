# Generated data (local)

`batch_queries_2k.jsonl` is produced by `manimator.batch.querygen` (see [`manimator/batch/README.md`](../manimator/batch/README.md)). It is gitignored once generated so the repo stays small; regenerate on each machine as needed.
