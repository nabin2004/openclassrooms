MANDATORY TOPIC TITLE: "Transformer Architecture". Do not substitute RNNs, attention mechanisms in isolation, or generic sequence models—the video must teach the complete Transformer architecture for sequence modeling.

Teach me Transformer Architecture in a visual, intuitive way for a short educational animation.

Audience: someone who knows basic neural nets and vectors but is new to attention-based models.

Cover these ideas across a small number of scenes (keep each scene focused—one main insight per scene):

1) Why Transformers matter: limitations of RNNs (sequential processing bottleneck); examples like machine translation, language models, or multimodal tasks where parallel processing and long-range dependencies are crucial.

2) The attention mechanism: query, key, value concept; show how one token attends to all others in parallel; visualize attention weights with a heatmap showing which tokens influence each computation.

3) Self-attention in action: same token can play different roles (query/key/value); demonstrate how the hidden representation for each position is a weighted combination of all input positions; contrast with RNN's strictly sequential flow.

4) Multi-head attention and feed-forward layers: multiple attention heads capture different relationships; feed-forward networks applied position-wise; stack these into layers for depth and expressiveness.

5) Positional encoding (briefly): how Transformers inject order information since there's no inherent sequence; show sinusoidal patterns or embedding visualization so parallel processing still respects position.

6) Wrap-up: one-sentence recap—Transformers use self-attention to process entire sequences in parallel, allowing each token to directly attend to all others while maintaining position awareness.

Style: 3Blue1Brown-like clarity, minimal clutter, consistent colors (e.g., BLUE for inputs, GOLD for attention weights, GREEN for outputs, PURPLE for query/key/value).

Output: concise total runtime (a few minutes of video), strong voiceover that matches what appears on screen. Every scene must include explicit narration text suitable for TTS.
